/**
 * seed.js
 * Initial sample data seeding
 */

import { TransactionRepository, CategoryRepository } from './db/repositories.js';
import { createTransaction } from './db/models.js';

export async function seedSampleData() {
  // Dummy data seeding removed per user request.
  // The user will start with a fresh app.
}
